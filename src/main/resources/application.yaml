server:
  port: 8080

library:
  name: ${LIBRARY_NAME:DEFAULT_LIBRARY_NAME}

spring:
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:mem:testdb
    driverClassName: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true

